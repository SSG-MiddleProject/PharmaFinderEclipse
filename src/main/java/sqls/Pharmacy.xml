<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Pharmacy">
<!--    내 주변 약국 리스트 count => ExtDto all count-->
    <select id="countPharmacyList" resultType="int" parameterType="ssg.middlepj.pharmafinder.dto.PharmacyParam">
        SELECT COUNT(*)
        FROM store
        WHERE dutyAddr LIKE CONCAT('%', #{Q0}, '%')
          AND dutyAddr LIKE CONCAT('%', #{Q1}, '%');
    </select>

<!--    내 주변 약국 리스트 ExtDtoList -->
    <select id="selectPharmacyList" parameterType="ssg.middlepj.pharmafinder.dto.PharmacyParam">
        SELECT *
        FROM store
        WHERE dutyAddr LIKE CONCAT('%', #{Q0} , '%')
          AND dutyAddr LIKE CONCAT('%', #{Q1} , '%')
    </select>

<!--    약국 상세 정보-->
    <select id="selectPharmacy" parameterType="ssg.middlepj.pharmafinder.dto.PharmacyParam">
        SELECT *
        FROM store
        WHERE dutyAddr LIKE CONCAT('%', #{Q0}, '%')
          AND dutyAddr LIKE CONCAT('%', #{Q1}, '%')
          AND dutyName LIKE CONCAT('%', #{QN}, '%')
    </select>

</mapper>
